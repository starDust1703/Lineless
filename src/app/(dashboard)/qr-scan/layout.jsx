export const metadata = {
  title: "Scan QR Code | LineLess",
  description: "Scan a queue QR code to instantly join a queue.",
};

export default function QRScanLayout({ children }) {
  return <>{children}</>;
}
