export const metadata = {
  title: "Dashboard | LineLess",
  description: "View and manage your queue status.",
};

export default function UserDashboardLayout({ children }) {
  return <>{children}</>;
}
