export const metadata = {
  title: "Queue Admin | LineLess",
  description: "Manage specific queue operations and participants.",
};

export default function QueueAdminLayout({ children }) {
  return <>{children}</>;
}
